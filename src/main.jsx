import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css'; // Assuming you have an index.css file for styles

ReactDOM.createRoot(document.getElementById('root')).render(<App/>); // Assuming App is imported from another file